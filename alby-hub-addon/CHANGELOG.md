# Changelog

## 0.0.7

- Cloud-Modus: Ingress liefert jetzt eine Info-Seite statt leerer/weißer Oberfläche
- Add-on-Option `nwc_connection_string` als Passwortfeld maskiert
- Dokumentation für Cloud-Ingress-Verhalten und NWC-Feldschutz aktualisiert

## 0.0.6

- Add-on-Dokumentation (`DOCS.md`) ergänzt
- NOSTR-Relay-Startskript (`nostr-relay/start.sh`) ergänzt
- Cloud-Modus-Selbsttests in `run.sh` erweitert (NWC `relay`/`secret` + Relay-Erreichbarkeit)
- Expert-Modus: optionales NOSTR-Relay als Proxy auf Port `3334`
- OCI-Source-URL in `build.yaml` auf korrektes Repository korrigiert
